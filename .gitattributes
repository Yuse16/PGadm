# Auto detect text files and perform LF normalization
* text=auto

# Markdown
*.md text diff=markdown

# Always use LF for shell scripts
*.sh text eol=lf

# Always use CRLF for PowerShell
*.ps1 text eol=crlf
