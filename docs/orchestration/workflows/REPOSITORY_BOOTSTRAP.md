# Prompt para iniciar fase

Entrega primero objetivo, packs, agentes, dependencias, historias, orden, ramas, worktrees, migraciones, pruebas, gates y riesgos. Después ejecuta únicamente la primera historia lista.
